const ALLOWED_EMAIL_DOMAIN = "@csub.edu";
const MIN_PASSWORD_LENGTH = 8;
const VERIFICATION_TOKEN_TTL_HOURS = 24;

module.exports = {
  ALLOWED_EMAIL_DOMAIN,
  MIN_PASSWORD_LENGTH,
  VERIFICATION_TOKEN_TTL_HOURS,
};
